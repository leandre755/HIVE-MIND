# Current Project Context

## 🏆 Major Milestones (Archived Epics)

- [2026-09-01] **Éradication 100% des Vulnérabilités npm (HIGH/MODERATE) & Formateur de Stickers Natif WebP/Exif (`src/utils/stickerFormatter.ts`)** : Suppression intégrale de `extract-zip` (paquet mort) et `wa-sticker-formatter` (arbre vulnérable de 173 sous-dépendances purgé), migration du plugin `create_sticker` vers un moteur natif basé sur `sharp` avec injection de chunks RIFF/EXIF, mise à jour de l'override `file-type` vers `^21.3.4`. Résultat : `npm audit` 0 vulnérabilité (0 High, 0 Mod, 0 Critical), `tsc --noEmit` 0 erreur, `oxlint` 0 warning, `depcruise` 0 violation, 502/502 tests unitaires passés.

- [2026-09-01] **Cartographie Exhaustive & Rapport d'Ingénierie des 26 Sous-Systèmes HIVE-MIND (`docs/subsystems_report.pdf`)** : Audit complet du codebase, identification de 26 sous-systèmes réimplémentables (Core, Cognition, Providers/SmartRouter, Transports, Runtime/Safety), rédaction modulaire LaTeX en 7 chapitres (`docs/chapters/`), compilation en document PDF de référence (89 pages, 1.04 Mo, métriques de Martin, contrats d'E/S, diagrammes TikZ vectoriels, 0 overfull hbox), vérification de 100% des 110 chemins de fichiers dans `src/`, 493/493 tests Jest validés et **VICTORY CONFIRMED** par l'Auditeur de Victoire indépendant.
- [2026-08-28] **Extraction & Découplage Complet de la TUI dans son Dépôt Standalone (`/home/omni/Code/HIVE-MIND-TUI`)** : Relocalisation de `HiveTransport.ts` dans `src/core/transport/tui/`, isolation 100% headless du Core HIVE-MIND, purge intégrale de 17 dépendances Ink/React, dépôt autonome `HIVE-MIND-TUI` créé (372 fichiers, 89/89 tests unitaires/adverses passés, 0 erreur TS/ESLint), migration complète de la doc/plans TUI, et validation E2E cross-process WebSocket (`src/tests/e2e/tui_cross_process_e2e.test.ts`, 11/11 tests passés, 72/72 suites Jest passées sans régression). Validé à 100% par l'Auditeur de Victoire indépendant.
- [2026-08-07] **Architecture Unified Context Regulation (HIVE-MIND)** : Audit critique d'incohérence & Red-Teaming (R1) et Spécification d'Architecture Cible SSOT (R2) achevés et validés à 100% par l'équipe multi-agents (Survey Explorers, Workers, Reviewers, Auditeur médico-légal CLEAN). `AUDIT_CRITIQUE_INCOHERENCE.md` et `UNIFIED_CONTEXT_MANAGER_SPEC.md` générés.
- [2026-07-24] Zero-Slop Execution Plan Created & Lots 1–3 completed.
- [2026-07-27] Elimination of all `sonarjs/cognitive-complexity` errors project-wide (0 errors, `tsc --noEmit` 0 errors).
- [2026-07-27] Complete Elimination of all `eslint-plugin-security` warnings project-wide (0 warnings, 0 TS errors, 100% tests passing).
- [2026-07-27] Complete Resolution of 234 TypeScript errors across 41 files in `src/tui/ui/` down to **0 errors** (`npx tsc --noEmit` clean).
- [2026-07-28] Session 1 de recovery complétée et commitée (`8fd2a14`).
- [2026-07-28] Session 2 de recovery complétée : éradication des 326 erreurs ESLint de `src/types/tui-globals.d.ts` (0 erreur, 0 warning) et refactorisation de `src/core/index.ts`.
- [2026-07-29] Session 3 de recovery complétée : `src/core/transport` passe de 340 problèmes à 0.
- [2026-07-30] Sessions 4, 5, 6, 7 complétées.
- [2026-08-06] **Architecture Smart Provider 2 Couches (Layer 1 x Layer 0) & Conversion GenerationParams** achevée et validée 100/100 OK (`npx tsc --noEmit` clean 0 erreurs, `npx eslint src/providers/` clean 0 erreurs 0 avertissements, 35/35 tests unitaires Jest PASS).
- [2026-08-06] **Menu d'Options UX Interactif CLI au Démarrage & Fix Pairing Code Baileys (Browsers.ubuntu)** achevé, audité par l'Équipe Multi-Agents et validé 100/100 OK (`npx tsc --noEmit` 0 erreur, `npx eslint src/cli/` 0 erreur 0 warning, 64/64 suites unitaires Jest PASS, 499/499 tests passés). Commits: `4af5f72`, `df59518`, `db96647`, `bf0f382`.
- [2026-08-06] **Fix ENOENT `session/creds.json` (use-after-delete Baileys)** : recherche multi-agents (5 agents codebase+internet) puis fix worker+critique adversarial. Cause racine = `disconnectWhatsApp()` supprimait `session/` juste après sa création par `useMultiFileAuthState`, avant l'écriture par `creds.update`. Validé 100/100 OK (`tsc` 0 err, `eslint` 0/0, 499/499 tests PASS). Verdict critique adversarial : APPROVED, 0 défaut.
- [2026-08-06] **Fix Rejet Pairing Code WhatsApp (timing handshake Noise)** : analyse comparative multi-agents du dépôt de référence `xhclintohn-Toxic-MD` (qui fonctionne). Cause racine = `requestPairingCode` appelé trop tôt (sur `connection.update='connecting'`, avant fin du handshake Noise) → IQ `link_code_companion_reg` rejeté silencieusement → aucune notification push. Fix = reproduction du pattern Toxic-MD (`await delay(3000)` avant `requestPairingCode`) + détection succès live. Validé statiquement 100/100 (`tsc` 0, `eslint` 0/0, 499/499 tests). Verdict critique : APPROVED.
- [2026-08-06] **Fix Restart 515 post-pairing + Détection UI + Titre Menu** : après validation du code pairing (notification reçue ✅), WhatsApp fermait la connexion avec le statut 515 (`restartRequired`) — comportement normal exigeant la recréation du socket. `handleConnectionClose` ne reconnectait QUE si `pairingRequested && !isRegistered` → au 515 `registered===true` → aucune branche exécutée → socket mort, `open` impossible → timeout 120s → « impossible de se connecter ». Fix = nouvelle branche `if (isRegistered) → onReconnect` (recréation socket, ne redemande pas le code car `!state.creds.registered` désormais faux). Validé RUNTIME par l'utilisateur (connexion réussie). Secondaire : faux négatif de détection UI corrigé par flush explicite `await saveCreds()` dans `finish(true)` avant résolution (le menu relisait un `creds.json` incomplet car le dernier `saveCreds` async n'était pas flushé). Titre menu remplacé « HIVE-MIND OPTIONS MENU UX » → « 🤖 HIVE-MIND » + panneau refactorisé (`formatStatusLine`, largeurs fixes `padEnd`). Validé 100/100 (`tsc` 0, `eslint` 0/0, 499/499 tests). **VALIDÉ RUNTIME par l'utilisateur : connexion WhatsApp + affichage UI « 🟢 Connecté » post-pairing tous deux confirmés.**
- [2026-08-06] **Retrait du QR code automatique du transport Baileys (anti-doublon menu)** : `baileys.ts connect()` affichait un QR auto au démarrage du bot core si aucune session n'existait — doublon avec le menu `startupMenu` qui pilote désormais l'auth. Retrait de l'import `qrcode`, `printQRInTerminal:true`→`false`, branche `if (qr)` et champ `qr` dans `_handleConnectionUpdate`. Ajout d'un guard `if (!state.creds.registered) throw` dans `connect()` → échec propre si transport démarre sans session (menu skippé en headless/Railway ou Skip sans connexion) au lieu d'un QR inutilisable. L'option QR du sous-menu + le mode QR de `whatsappAuthHelper` RESTENT (auth pilotée par l'utilisateur). `QR_RECEIVED` n'avait aucun subscriber (publish déjà mort). Validé 100/100 (`tsc` 0, `eslint` 0/0, 499/499 tests).
- [2026-08-07] **Réécriture complète du System Prompt hive-mind** (91→301 lignes) via flotte de 11 agents : 8 agents de débat adversarial sur 435 system prompts de l'industrie (174K lignes, Anthropic/OpenAI/Google/xAI/Meta/Kimi/Microsoft/Cursor/DeepSeek/Mistral/Perplexity), 1 agent de synthèse guide (GUIDE_FINAL.md 1339 lignes), 1 agent d'analyse codebase (87+ outils, 10 providers), 1 agent Prompt Builder. Nouveau prompt : sandwich safety (top+bottom), anti-sycophancy, anti-meta-language, channel protocol (WA/TG/Discord/CLI/TUI), tool governance structuré, sub-agent protocol (Worker≠Checker), anti-hallucination, error handling classifié, scope fidelity. Backup `system.md.bak` conservé. Validé `tsc --noEmit` 0 erreurs. Non-committé.
- [2026-08-07] **Epic MessageConverter Unifié + ConfigValidator (F1, F7, F8, F9)** : Architecture unifiée de conversion pivot↔wire éliminant la duplication inter-adapters. `messageConverter.ts` (610 lignes, 8 fonctions) + `configValidator.ts` (325 lignes, 3 fonctions). Adapters `anthropic.ts`, `openai.ts`, `groq.ts`, `AnthropicCompatibleProtocol.ts` refactorisés pour déléguer au converter. Gain incident : `reasoningContent` propagé dans openai/groq (absent avant). ConfigValidator intégré dans ModelRegistry (warn-only, pas de dépendance circulaire). Audit adversarial : 6 défauts tous corrigés (D-000 CRITIQUE conversion images restaurée, D-001/D-002 MEDIUM warnings ajoutés, D-004/D-005 LOW hardcodés retirés). Validé `tsc --noEmit` 0 erreurs, `eslint src/providers/` 0 erreurs 0 warnings.
- [2026-08-07] **Audit & Fixes Architecturaux Providers (8 fixes chirurgicaux)** : Audit multi-agents (3 explorers + auditeur médico-légal) du système providers, identification de 11 findings (F1-F11). Application de 8 fixes chirurgicaux : F2 (tool_call_id Cohere), F3 (suppression codestral-latest de mistral), F4 (console.warn collision ModelRegistry), F5 (guard gemini-native ExecutionLayer), F6 (randomUUID gemini.ts), F10a/F10b (suppression fallbacks morts gpt-5.5/gemini-3.1-pro-high/claude-opus-4-6-thinking), F11 (JSDoc sanitizeToolIdsInMessages). Refactoring complexité cognitive (extraction helpers ensureProtocolSupported, warnIfCollision, resolveHeaderFamily). Validé `tsc --noEmit` 0 erreurs, `eslint` 0 erreurs/warnings. Epic architectural (MessageConverter unifié F1/F7/F8/F9) documenté dans plan, non démarré.
- [2026-08-07] **Fix perte de session WhatsApp après arrêt/restart** : enquête multi-agents (4 agents de traçage lifecycle + 3 hypothèses + vérification adversariale) identifiant 5 causes racines → 5 fixes workers + 4 reviewers adversariaux + 1 audit final (12 agents total, 458K tokens). Causes racines : (1) `process.exit(0)` sans flush dans `os_shutdown` et `shutdown_bot`, (2) `disconnectWhatsApp()` dans le catch global détruisant le dossier `session/` sur erreur transitoire, (3) `saveCreds()` non flushé avant `sock.end()` dans `disconnect()`, (4) try/catch unique dans `shutdown()` laissant le socket ouvert sur erreur, (5) `sendPresenceUpdate` bloquant `sock.end()`. Fixes : SIGTERM unifié (system + dev_tools), try/catch isolés par étape (hive-mind.ts), flush creds avant fermeture socket (baileys.ts), suppression conditionnelle `session/` avec guard `registered` (whatsappAuthHelper.ts). Validé : `tsc --noEmit` 0 erreurs, `eslint` 0/0, audit 5 invariants A-E tous PASS.
- [2026-08-08] **6 Cascades de défaillance long-running corrigées & Commité (`0420867`)** : analyse multi-agents identifiant 6 cascades (wake storm MailboxWatcher, boucle 440 Baileys, amnésie TTL WorkingMemory, zombies PersistentShell, callbacks dupliqués PluginLoader, 429 cascade QuotaManager+CircuitBreaker). Audit npm résolu (substitution `node-gtts` → `google-tts-api`, 0 vulnérabilités high/critical). Commit `0420867` effectué sans `--no-verify` et validé statiquement (`tsc --noEmit` 0 erreurs).


## 🎯 Objective

Assurer une qualité de code Zero-Slop irréprochable et consolider la nouvelle architecture Smart Provider à 2 Couches.

## 🧠 Decisions Made

- [2026-09-01] **Décommissionnement et suppression du workflow Jules PR Review (`.github/workflows/pr-review.yml`)**
  - **Context**: Directive explicite du mainteneur (« supprime l'action PR review par jule »). Le workflow `pr-review.yml` utilisait l'action tierce `sanjay3290/jules-pr-reviewer` et un fichier de règles `JULES.md` inexistant, tout en violant les critères de sécurité de `.github/scripts/verify_workflows.py` (`timeout-minutes` manquant, SHA non pinnées).
  - **Discarded Options**: Conserver le workflow en retirant seulement le step Jules (rejeté par l'utilisateur lors de l'interview d'alignement au profit d'une suppression intégrale) ; ignorer les violations de `verify_workflows.py` (rejeté : viole la qualité CI).
  - **Rationale**: Suppression intégrale de `.github/workflows/pr-review.yml`, alignement de `.gouvernance/GOVERNANCE.md` (retrait du tableau des workflows et de la liste des bots requis) et de la dette technique dans `.GCC/main.md`. Résultat : 7/7 workflows GitHub Actions 100% conformes à `verify_workflows.py` (0 warning, 0 erreur).

- [2026-09-01] **Éradication Intégrale des Vulnérabilités npm (HIGH/MODERATE) & Formateur de Stickers Natif (`sharp`)**
  - **Context**: L'audit de sécurité des dépendances npm révélait 19 vulnérabilités dont 4 de sévérité HIGH (`extract-zip` GHSA-jmr9-qjv8-65gv, `image-size` GHSA-w3rx-r6r6-pgpr & GHSA-5p2g-fcmc-qvqq, `jpeg-js` GHSA-xvf7-4v9q-58w6 & GHSA-w7q9-p3jq-fmhm) et 15 de sévérité MODERATE (`phin`, `qs`, `tough-cookie`, `uuid`, `file-type`). L'analyse d'impact a démontré que `extract-zip` était un paquet mort non importé, et que `wa-sticker-formatter` (obsolète et non maintenu) était l'unique responsable de l'introduction de 18 vulnérabilités via son arbre de dépendances (`jimp`, `gif-frames`, `request`, `image-size`, `jpeg-js`).
  - **Discarded Options**: Conserver `wa-sticker-formatter` et tenter de forcer des `overrides` npm (rejeté : `image-size` et `jpeg-js` n'avaient aucun correctif amont compatible avec l'arbre hérité de `wa-sticker-formatter`) ; désactiver l'audit de sécurité ou baisser les seuils de la gate (rejeté : violation stricte des règles de sécurité et de qualité).
  - **Rationale**: (1) Désinstallation complète de `extract-zip` et `wa-sticker-formatter` de `package.json` et `package-lock.json` (173 paquets purgés). (2) Développement d'un formateur de stickers natif ultra-performant et sécurisé `src/utils/stickerFormatter.ts` basé sur `sharp` (déjà présent dans le projet) et injection directe de chunks RIFF/EXIF compatibles WhatsApp. (3) Mise à jour de l'override `file-type` de `^16.5.4` vers `^21.3.4` pour corriger la vulnérabilité DoS de l'analyseur ASF. (4) Validation complète : `npm audit` rapporte 0 vulnérabilité (0 Critical, 0 High, 0 Moderate, 0 Low), `npm run build` 0 erreur, `npm run lint:fast` 0 warning, 502/502 tests unitaires validés (65/65 suites).


- [2026-09-01] **Usage prioritaire obligatoire du serveur MCP `codebase-memory-mcp` par tous les agents**
- **Context**: Directive explicite du mainteneur : tous les agents d'ingénierie et de développement opérant sur HIVE-MIND doivent utiliser en priorité absolue les outils du serveur MCP `codebase-memory-mcp` (`search_graph`, `trace_path`, `get_code_snippet`, `check_index_coverage`, `query_graph`, `get_architecture`, `index_repository`) pour toute exploration, analyse d'impact, traçage d'appels et navigation dans le code source, au détriment des outils d'exploration textuelle conventionnels du harnais (`grep_search`, `find_by_name`, scans de fichiers bruts).
- **Discarded Options**: Conserver `grep`/`find` comme méthode primaire (rejeté : exploration superficielle sans compréhension du graphe de dépendances ni de la hiérarchie des types) ; réserver MCP aux seules sessions de refactoring lourd (rejeté : directive générale permanente).
- **Rationale**: Le graphe de connaissances sémantique de `codebase-memory-mcp` garantit la précision des graphes d'appels (`trace_path`), la détection des symboles orphelins et la couverture exacte (`check_index_coverage`), éliminant les angles morts et les suppositions d'architecture. Le repli sur `grep_search` / `find_by_name` est strictement circonscrit aux chaînes littérales de configuration, logs d'erreurs textuels, Dockerfiles et scripts shell non indexés dans le graphe.

- [2026-08-31] **AGENTS.md §6 aligné sur les hooks réels, badge Node du README, titulaire de LICENSE renseignée**
- **Context**: Interrogé sur le mot « dérive », le session agent a mesuré au lieu d'affirmer : dans `AGENTS.md`, `gitleaks` apparaissait **0 fois** et `ALLOW_CONFIG_EDIT` **0 fois**, alors que §6 est le seul endroit où un agent apprend l'existence de la gate — un agent qui respecterait littéralement « Never use `--no-verify` » sans connaître le canal autorisé se retrouve bloqué dès qu'un des 8 fichiers protégés doit être corrigé. S'y ajoutaient deux contradictions signalées et non traitées : badge `Node.js-18+` (`README.md` l.26) et prérequis « 18.x LTS » (l.391) contre `package.json` `"node": ">=22.0.0"`, et une `LICENSE` **sans titulaire** depuis le retrait de la ligne MIT. Le mainteneur a alors ordonné la correction, avec deux contraintes explicites : `AGENTS.md` doit rester **intégralement en anglais**, et le titulaire doit être son identifiant GitHub. Cette même phrase constitue l'approbation explicite qu'exige l'invariant 4 pour toucher un fichier de politique.
- **Discarded Options**: Rédiger §6 en français (la première proposition l'était — rejetée sur demande explicite ; le fichier est lu par des agents multilingues et son reste est anglais) ; écrire en clair l'en-tête d'un bloc de clé privée pour décrire la couche 2 de la gate (rejeté : ce scan n'a **aucune** exemption de chemin et s'applique au markdown, `AGENTS.md` compris — le format doit être nommé, pas reproduit, sinon la retouche de la règle documente sa propre violation) ; caler le badge sur `Node 20` pour coller au `node-version: '20'` du workflow de release (rejeté : la source de vérité déclarée du projet est `engines.node`, le workflow est une dette distincte et `.github/**` est hors limites sans arbitrage) ; **remplir les crochets de l'annexe Apache (option explicitement écartée plus bas dans ce journal, entrée « Repassage du dépôt de MIT à Apache-2.0 » — revirement décidé par le mainteneur ce tour, consigné ici plutôt que d'éditer la décision historique)**.
- **Rationale**: §6 décrit maintenant les contrôles tels qu'ils s'exécutent : gardes d'index, deux scans littéraux égouttés côté markdown, scan d'en-tête de bloc de clé privée sans aucune exemption de chemin, `gitleaks` bloquant sur l'index, sortie anticipée sans JS/TS, puis les trois étapes du push ; et deux blocs nouveaux nomment explicitement les 8 fichiers protégés avec `ALLOW_CONFIG_EDIT=1` comme canal **autorisé et non contourné** (`--no-verify` reste interdit par §5.3), plus `gitleaks` comme dépendance d'exécution de la gate. Preuves : comptage de tokenizer français sur `AGENTS.md` → 0 ; `git diff --stat` → `AGENTS.md | 8 ++++++--`, `README.md | 4 ++--`, `LICENSE | 2 +-` ; sha256 `LICENSE` → `d10f91a1eb210bb37509cc5f7e55568bfd92175a6bf05ae78cf810bd3269658b` contre `cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30` pour le texte distribué. L'annexe d'Apache-2.0 est un guide d'application et non une clause : le remplir est l'usage prévu, et la divergence de somme est la conséquence assumée — aucune clause ne change. Année retenue : 2026, date du premier commit `006e4ae`. Ce qui reste au mainteneur : `node-version: '20'` de `.github/workflows/release.yml:43`, incohérent avec `engines >= 22` mais non bloquant faute de `.npmrc` (`engine-strict` désactivé).

- [2026-08-31] **Quality Gate : scans littéraux limités au code et gitleaks rendu bloquant**
- **Context**: Le faux positif structurel qui avait contraint au `--no-verify` de `84e61d2` bloquait désormais tout commit touchant la documentation de politique ou les journaux `.GCC/`. Le mainteneur a demandé de lever le blocage et d'installer/exploiter gitleaks dans le `pre-commit` et le `pre-push`.
- **Discarded Options**: Filtrer par liste blanche d'extensions de code (rejeté : toute extension absente de la liste deviendrait un angle mort silencieusement non scanné, alors qu'un simple rejet de `\.(md|markdown)$` ne ferme que la classe connue comme documentaire) ; garder gitleaks optionnel derrière un `command -v` comme auparavant (rejeté : c'est exactement le mode de défaillance subi — le binaire était absent du poste, le contrôle sautait sans bruit) ; déposer un `.gitleaksignore` (rejeté : les empreintes sont liées aux commits et se multiplient à chaque réécriture, alors que l'`[allowlist]` de chemin est stable) ; supprimer les scripts morts `check-format.sh` et `run-linter.sh` de `_common/` (rejeté : hors périmètre, signalé comme dette).
- **Rationale**: Trois garde-fous au lieu d'un. (1) `DOC_FILTER='\.(md|markdown)$'` égoutte les deux scans littéraux de `.githooks/pre-commit`. (2) Un scan d'en-tête de bloc PEM **sans aucune exemption de chemin** est ajouté, avec les variantes RSA / DSA / EC / OPENSSH / ENCRYPTED / PGP : la compensation ne dépend pas du type de fichier. (3) `gitleaks` 8.30.1 (binaire officiel vérifié par sa checksum publiée, installé dans `~/.local/bin` hors dépôt) devient bloquant via `.githooks/_common/detect-secrets.sh` à deux vitesses — `staged` appelé par `pre-commit`, `history` appelé par `pre-push` — ce qui élimine au passage le seul script de secrets mort de `_common/`. `.gitleaks.toml` étend les règles natives (`useDefault = true`) et n'excipe que `documentations/tui/reference/core-connection.md`, dont les deux blocs JSON signalés sont des jetons d'exemple de 8 lettres ; le fichier de configuration est ajouté à la liste `PROTECTED_MODIFIED` puisqu'il peut désarmer la détection. Vérifications réelles : canary PEM factice indexé dans un `.md` → rejet (couche 2) puis rejet indépendant par gitleaks (couche 3) ; `gitleaks git` sur les 8 commits → « no leaks found » ; le snippet d'installation affiché quand le binaire manque a été exécuté tel quel dans un environnement factice et a réussi. Test croisé du filtre : un `.md` citant `eslint-disable` et `@ts-ignore` en prose passe, un `.ts` portant une vraie `// eslint-disable-next-line` reste rejeté — le contrôle n'est pas désarmé, il est re-périmétré. Contrepartie éditoriale à connaître : la couche 2 n'ayant **aucune** exemption de chemin, un journal qui reproduit littéralement un en-tête de bloc PEM se fait refuser ; il doit **nommer** le format, pas l'écrire en clair.

- [2026-08-31] **Repassage du dépôt de MIT à Apache-2.0**
- **Context**: Demande du mainteneur (« recrit tout enfaite » + texte Apache 2.0 collé) après la suppression de la ligne de copyright de la licence MIT (`d4ab8d3`). Le fichier `LICENSE` devient le texte Apache-2.0 canonique.
- **Discarded Options**: Conserver MIT et n'ajouter un en-tête Apache que pour les contributions externes (rejeté : une seule licence par dépôt, sinon ambiguïté de redistribution) ; remplir les champs `[yyyy] [name of copyright owner]` de l'annexe avec le nom du mainteneur (rejeté **à ce moment-là** : l'attribution venait précisément d'être retirée du fichier de licence ; l'annexe est un guide d'application, pas une clause, et le texte canonique distribué par apache.org conserve les crochets. **Revirement le même jour sur ordre explicite du mainteneur** — voir l'entrée « titulaire de LICENSE renseignée » ci-dessus ; ne pas restaurer les crochets) ; ne pas toucher `README.md` (initialement rejeté : fichier réservé au mainteneur — **interdit levé par sa demande explicite en fin de session**, badge l.25, section Licence l.749 et signature l.754 alignés depuis).
- **Rationale**: `LICENSE` réécrit puis **vérifié octet pour octet** contre `https://www.apache.org/licenses/LICENSE-2.0.txt` (`diff` vide, sha256 commun `cfc7749b96f63bd31c3c42b5c471bf756814053e847c10f3eb003417bc523d30` ; **état caduc** : cette égalité a pris fin quand l'annexe a été renseignée le même jour, la somme courante est consignée dans l'entrée la plus récente de ce journal) ; deux dérives détectées et corrigées par ce diff : « The Derivative Works » → « the Derivative Works » (§4c) et ligne blanche initiale manquante. `package.json` aligné sur l'identifiant SPDX `Apache-2.0` (l.41), JSON revalidé par `node -e require()`. Conséquence juridique assumée : Apache-2.0 impose la notice de modification (§4b) et accorde une licence de brevet avec résiliation en cas de poursuites (§3), contrairement à MIT — résumée dans la section Licence du README. Attribution : `"author": "leandre755"` (l.40), qui est aussi l'identité Git du dépôt ; le pseudo retiré n'apparaît plus dans aucun fichier suivi après la mise à jour du pied de page README.

- [2026-08-31] **Baseline de gouvernance codée en deux commits, dont un avec `--no-verify` autorisé**
- **Context**: Le déploiement de la baseline de gouvernance (6 piliers) produisait 85 changements. Deux groupes cohérents ont été isolés : `0ead9b5` (nettoyage : dé-suivi des notes personnelles et configs clients, `.gitignore` réécrit avec exception de suivi `.GCC/`, husky décommissionné) puis `84e61d2` (les 62 fichiers de gouvernance : AGENTS.md, ARCHITECTURE.md, `.GCC/`, `.gouvernance/`, `.githooks/`, `.github/`). Le second commit a été **rejeté** par la gate : les deux scans littéraux du `pre-commit` (`STAGED_SECRETS`, `STAGED_SUPPRESSIONS`) raisonnent fichier par fichier sur l'intégralité du patch et signalent 12 fichiers markdown qui *citent* ces motifs en prose — dont `AGENTS.md` via une ligne déjà supprimée de sa version précédente (0 occurrence dans le fichier de travail, donc aucune réédition possible).
- **Discarded Options**: Exclure `.GCC/branches/*` et `AGENTS.md` du commit (rejeté : AGENTS.md est la pièce maîtresse de la baseline et le blocage se reproduirait au premier commit suivant) ; paraphraser les 11 journaux de session historiques (rejeté : dégraderait la fidélité des archives sans résoudre le cas `AGENTS.md`) ; modifier le périmètre des scans immédiatement (rejeté : `.githooks/**` est un fichier de politique, invariant 4, et le mainteneur a choisi de reporter).
- **Rationale**: Le mainteneur a explicitement autorisé `--no-verify` **pour ce commit uniquement** et demandé que la correction soit documentée pour la session suivante. Écart de procédure assumé et tracé : `AGENTS.md` §5.3 interdit par ailleurs ce drapeau. Preuves rassemblées avant commit : aucun fichier JS/TS indexé (les contrôles 3/8 à 8/8 n'auraient rien examiné), scan strict de bloc PEM sur tout l'index → 0 résultat, `gitleaks` absent du poste. Filtre correctif validé rétrospectivement sur `HEAD~1..HEAD` : les deux scans ramenés à 0 fichier avec `grep -vE '\.(md|markdown)$'`. Détail complet et chemins de propagation dans `.GCC/resume.md`.

- [2026-08-07] **Unification du shutdown via SIGTERM (session WhatsApp persistence)**
- **Context**: Trois chemins d'arrêt du bot (Ctrl+C SIGINT, `shutdown_bot` dev_tools, `os_shutdown` plugin) n'assuraient pas le flush de `creds.json` avant `process.exit()`. `os_shutdown` et `shutdown_bot` appelaient directement `process.exit(0)` dans un `setTimeout`, contournant la chaîne de cleanup de `hive-mind.ts`. Résultat : perte de session WhatsApp après arrêt/restart.
- **Discarded Options**: Flush explicite dans chaque handler (rejeté : duplication de la chaîne SIGTERM déjà complète dans `hive-mind.ts`) ; `await saveCreds()` global partout (rejeté : ne couvre pas le disconnect socket ni le releaseLock).
- **Rationale**: (1) `shutdown_bot` et `os_shutdown` appellent désormais `process.kill(process.pid, 'SIGTERM')` → déclenche le handler SIGTERM de `hive-mind.ts` → flush DB → saveCreds → disconnect → releaseLock → exit. (2) Le handler `shutdown()` isole chaque étape dans son propre try/catch pour qu'une erreur n'empêche pas les suivantes. (3) `sendPresenceUpdate` wrappé dans un try/catch interne dans `baileys.ts disconnect()` pour garantir que `sock.end()` est toujours exécuté. (4) `whatsappAuthHelper.ts` catch : suppression conditionnelle de `session/` uniquement si `!state.creds.registered` (préservation sur erreur transitoire). Validation : `tsc` 0, `eslint` 0/0, audit invariants 5/5 PASS.

- [2026-08-06] **Retrait du QR code automatique du transport Baileys (suppression du doublon avec le menu)**
- **Context**: Le transport `baileys.ts connect()` affichait un QR code automatiquement au démarrage du bot core si aucune session valide n'existait — comportement hérité d'avant l'ajout du menu `startupMenu`. Avec le menu interactif qui pilote désormais l'authentification (QR ou pairing code au choix via `whatsappAuthHelper`), ce QR auto devenait un doublon : il se déclenchait quand l'utilisateur skip le menu sans connexion, ou en mode headless/Railway où le menu est skippé automatiquement, affichant un QR inutilisable en mode serveur.
- **Discarded Options**: Conserver le QR auto comme secours Railway (rejeté : un QR dans les logs Railway ne peut être scanné, c'est du bruit — l'auth se fait via le menu en TTY local une fois pour toutes) ; retirer aussi l'option QR du sous-menu (rejeté : l'utilisateur a explicitement demandé de garder le menu avec l'option QR, seul le comportement auto à l'ancienne est visé) ; retirer la définition `QR_RECEIVED` de `events.ts` (rejeté : hors scope, fichier adjacent non strictement nécessaire, la définition morte est inoffensive).
- **Rationale**: (1) Retrait de l'import `qrcode`, `printQRInTerminal:true`→`false`, branche `if (qr)` et champ `qr` supprimés dans `_handleConnectionUpdate`. (2) Guard `if (!state.creds.registered) throw` dans `connect()` → échec propre avec message clair si le transport démarre sans session, au lieu d'un QR mort. (3) `QR_RECEIVED` n'avait aucun subscriber (publish déjà mort). (4) L'option QR du sous-menu `startupMenu` + le mode QR de `whatsappAuthHelper` RESTENT intactes (auth pilotée par l'utilisateur). Les reconnexions 515/440 passent le guard (`creds.registered` reste `true`). Validé statiquement (tsc/eslint/499 tests).

- [2026-08-06] **Gestion du Restart 515 (`restartRequired`) post-pairing WhatsApp**
- **Context**: Après validation du code pairing sur le téléphone, `creds.update` passe `registered` à `true`, PUIS WhatsApp ferme la connexion avec le statut 515 (`DisconnectReason.restartRequired`). C'est un comportement normal du protocole multi-device : le socket DOIT être recréé pour finaliser la session authentifiée. L'ancienne logique `handleConnectionClose` ne reconnectait QUE si `pairingRequested && !isRegistered` — au moment du 515 `registered===true` → aucune branche exécutée → aucun nouveau socket → attente d'un `connection==='open'` impossible → timeout 120s → « impossible de se connecter à cet appareil ». Confirmé par la lecture du dépôt de référence Toxic-MD (`index.js:1101`) qui gère explicitement le 515 dans sa clause de reconnexion via `startToxic()` sans condition sur `registered`.
- **Discarded Options**: Ignorer le 515 et augmenter le timeout (rejeté : le socket est mort, aucun `open` n'arrivera jamais quelle que soit la durée) ; traiter le 515 comme un `loggedOut`/fail (rejeté : le 515 est un restart attendu après succès pairing, pas une déconnexion réelle) ; ne reconnecter que sur `reason===515` explicite (rejeté : `isRegistered` couvre le cas nominalement sans dépendre d'un code raison spécifique fragile face aux variantes de Baileys).
- **Rationale**: Ajout d'une branche `if (isRegistered) { setTimeout(onReconnect, 1500); return; }` dans `handleConnectionClose` avant les autres conditions. À la reconnexion via `startSock()`, le guard `mode==='pairing' && !pairingState.requested && !state.creds.registered` (l.253) devient faux → aucun redemande de code → le nouveau socket se connecte directement avec les creds authentifiées → `connection==='open'` + `isRegisteredLive()` → `finish(true)`. Validation runtime utilisateur : connexion réussie.

- [2026-08-06] **Flush disque explicite de `creds.json` pour fiabiliser la détection UI**
- **Context**: Après `finish(true)` (connexion réussie), le menu principal relit immédiatement `session/creds.json` via `isWhatsAppConnected()` (exige `registered===true && me.id`). Or le dernier `creds.update` (contenant `me.id`) est persisté de façon asynchrone par le listener `saveCreds` — au moment du rendu du panneau, le flush disque n'était pas terminé → lecture d'un `creds.json` incomplet → faux « 🔴 DÉCONNECTÉ » alors que la session était valide. Vérifié empiriquement : post-connexion, `creds.json` contient bien `registered:true` + `me.id`.
- **Discarded Options**: Ajouter un `delay` fixe avant le rendu menu (rejeté : les règles interdisent les delay fixes pour les race conditions async, préférer les signaux explicites) ; poller `creds.json` jusqu'à ce que `me.id` apparaisse (rejeté : complexité inutile, le flush awaited résout la course à la source).
- **Rationale**: Dans `finish`, sur succès uniquement, appel `await saveCreds()` AVANT `resolvePromise(result)`. `saveCreds` sérialise l'état courant en mémoire (`state.creds`, déjà muté par Baileys à l'open) vers le disque de façon awaited → garantit qu'au retour du menu, `creds.json` est complet. `finish` devient async interne (`void finalize()`) sans casser l'API `Promise<boolean>`. Les anciens faux positifs (« connecté » alors qu'échec) déjà couverts par la détection stricte `registered===true && me.id`.

- [2026-08-06] **Correction du Bug use-after-delete ENOENT `session/creds.json`**
- **Context**: `useMultiFileAuthState` (Baileys v6.7.23) crée le dossier `session/` puis n'écrit `creds.json` qu'au premier événement `creds.update` (post-pairing). Or `whatsappAuthHelper.ts` appelait `disconnectWhatsApp()` (→ `safeRemoveDirectorySync`) 3 lignes après l'init quand `registered===false` (toujours vrai en première auth), supprimant le dossier avant l'écriture → `ENOENT` au moment où l'utilisateur saisit le numéro pour recevoir le code. Gap secondaire : `baileys.ts connect()` n'assurait jamais le dossier avant l'appel (échec sur volume Railway vide).
- **Discarded Options**: Migration vers `useSqliteAuthState` (rejeté : refactoring majeur hors scope, `useMultiFileAuthState` déprécié mais fonctionnel) ; purge conditionnelle anticipée avec lecture/parsing de `creds.json` (rejeté : ajoute de la logique de parsing sans bénéfice, la suppression du bloc suffit) ; simple `safeMkdirSync` sans supprimer le bloc purge (rejeté : le dossier était re-supprimé juste après).
- **Rationale**: (1) `safeMkdirSync(WA_SESSION_DIR, {recursive:true})` idempotent AVANT `useMultiFileAuthState` dans `whatsappAuthHelper.ts` et `baileys.ts`. (2) Suppression totale du bloc use-after-delete `if (!state.creds.registered) { disconnectWhatsApp(); }` (lignes 210-212) — la purge propre reste garantie par `finish()` (après retrait des listeners) et `catch()`. (3) `authSessionManager.ts` non modifié (déjà robuste). Réutilisation des wrappers `safeFs.ts` existants, imports ESM `.js`.

- [2026-08-06] **Validation Réseau en Direct des Jetons & Correction de l'Architecture de Couplage Baileys**
- **Context**: 1. `requestPairingCode` Baileys échouait avec HTTP 428 (`Connection Closed`) si le numéro était demandé dans l'événement du socket (timeout de connexion en arrière-plan pendant la saisie humaine) et si le `browser` était un tuple personnalisé inconnu de WhatsApp. 2. Les jetons Telegram/Discord étaient marqués "Connectés" sans contrôle réseau préalable.
- **Discarded Options**: Conserver la saisie du numéro dans l'événement socket avec un timer d'attente (rejeté : le socket expire pendant la saisie de l'utilisateur) ; utiliser un tuple de browser libre (rejeté : WhatsApp bloque les pairings non reconnus).
- **Rationale**: (1) Demande synchrone du numéro de téléphone de manière découpée *avant* l'instanciation de `makeWASocket()`. (2) Utilisation du signature `Browsers.ubuntu('Chrome')` natif de Baileys. (3) Validation réseau en direct via requêtes `fetch` sécurisées par `AbortSignal.timeout(8000)` auprès de l'API Telegram (`/getMe`) et Discord (`/users/@me`) avant tout enregistrement dans `.env`. (4) Nettoyage des dossiers de session par les wrappers sûrs `safeRemoveDirectorySync` de `safeFs.ts`.

## 🐛 Known Bugs / Technical Debt

- [2026-08-31] **Le workflow de release construit sur un runtime que le dépôt déclare interdit**
- **Fichiers concernés**: `.github/workflows/release.yml` l. 43 (`node-version: '20'`) contre `package.json` l. 38 (`"node": ">=22.0.0"`), `AGENTS.md` §2 (`Node.js >= 22`) et `README.md` (badge l. 26, tableau « Prerequisites » l. 391 — alignés sur 22 cette session). (L'ancien workflow `pr-review.yml` utilisait `lts/*`, désormais décommissionné).
- **Symptôme**: la publication s'effectue sur un runner que le `engines` du paquet rejette. Non bloquant en pratique : aucun `.npmrc` n'existe dans le dépôt, donc `engine-strict` est désactivé et `npm install` ne rend qu'un avertissement. Le risque réel est un artefact publié depuis un runtime non supporté.
- **Fix requis** (arbitrage mainteneur, `AGENTS.md` invariant 4 — `.github/**` n'est pas modifiable par l'agent sans accord explicite): soit remonter `node-version` à `'22'` (ou `lts/*`), soit abaisser `engines.node`. Les deux fichiers ne doivent pas rester en contradiction.

- [2026-08-31] **Faux positifs structurels de la gate sur la documentation markdown** — ✅ **RÉSOLU le 2026-08-31** (commit `cc4fa2a`, voir la décision « Quality Gate : scans littéraux limités au code et gitleaks rendu bloquant »)
- **Fichiers concernés**: `.githooks/pre-commit` (`STAGED_SECRETS`, `STAGED_SUPPRESSIONS`) et les variantes de hook livrées par le modèle de workspace (note de propagation conservée hors de ce dépôt).
- **Symptôme (historique)**: Tout commit indexant `AGENTS.md` ou un `.md` de `.GCC/branches/` était rejeté : les motifs de jeton/inhibition étaient cherchés dans tout le patch, suppressions et contexte compris, et la documentation de politique nomme ces motifs pour les interdire. `AGENTS.md` était infranchissable quelle que soit la réécriture de son contenu.
- **Correction appliquée**: filtre `\.(md|markdown)$` sur les deux scans littéraux + scan d'en-tête PEM sans exemption de chemin + gitleaks bloquant (index au `pre-commit`, historique au `pre-push`). Le contournement `--no-verify` n'est plus requis.
- **Constat annexe (toujours ouvert)**: sur les 4 scripts de `.githooks/_common/`, `check-format.sh` et `run-linter.sh` ne sont référencés par aucun hook — code mort. `detect-secrets.sh` est désormais branché sur les deux hooks ; les deux autres restent à supprimer ou à câbler.

- [2026-08-31] **Chemins absolus machine dans la documentation et le code suivis**
- **Fichiers concernés**: trois populations, mesurées sur l'état commité `4159a63`. **(1) Les 120 liens `file:///…/HIVE-MIND-RAILWAY/…`** siègent dans **16 fichiers** : 6 de `documentation/explanations/` (77 liens) et 10 de `documentations/tui/` (43 liens) — aucun lien hors de ces deux arbres. **(1b) 9 fichiers ne mentionnent le chemin qu'en prose** (`cd … && …`, commandes de journal) : 6 journaux `.GCC/` (`.GCC/branches/plan_generation_capabilities.md`, `plan_lint_recovery_8sessions.md`, `plan_restructuration_globale.md`, `test_afaire.md`, plus `main.md` et `resume.md`), plus `documentation/00_index.md`, `src/scripts/test_email.ts` et `src/tests/unit/utils/helpers.test.ts`. Les 16 + 9 = 25 fichiers contenant la chaîne littérale, c'est de là que venait l'estimation erronée décrite en rectification. **(2) 21 chemins machine absolus hors Railway** dans 11 fichiers, mesurés systématiquement : **8 occurrences dans 6 fichiers de code et de doc** (le lot traitable — `src/providers/adapters/codex.ts` l. 26 : `const AUTH_FILE_PATH = '/home/omni/.codex/auth.json'`, `src/scripts/test_codex_connection.ts`, `src/tests/e2e/bot.e2e.test.ts`, `src/tests/unit/plugins/bashTool.test.ts` ×2, `src/tests/unit/utils/helpers.test.ts` l. 27, `documentation/explanations/distribution_hive_mind.md` ×2) et **le solde, descriptif, dans 5 journaux `.GCC/`** (`test.md`, `main.md`, `test_afaire.md`, `plan_tui_render_loop_bug.md`, `resume.md` — dont le seul lien `file:///…/.gemini/antigravity-ide/brain/…` du lot). Le décompte par journal n'est **pas consigné** : il dérive à chaque retouche de journal — la présente entrée en a elle-même modifié la valeur — et ces fichiers sont des records, pas une cible de réécriture. À re-mesurer si besoin : `for f in .GCC/main.md .GCC/resume.md .GCC/branches/*.md; do printf "%s %s\n" "$f" "$(grep -o '/home/[A-Za-z0-9._-]*' "$f" | wc -l)"; done`.
- **Symptôme**: la population 1 vise un dépôt absent (ancien nom de la branche de déploiement Railway). Audit mesuré : ces 120 instances visent **65 cibles distinctes**, dont **45 se résolvent telles quelles** en chemin relatif dans ce dépôt et **20 sont introuvables** — 19 fichiers `src/tui/**`, déplacés vers le dépôt TUI autonome, plus `PROJECT.md` (dé-suivi). Dans la population 2, un seul chemin a un effet à l'exécution : `AUTH_FILE_PATH` de `codex.ts`, qui résout un jeton depuis le compte d'un particulier.
- **Rectification de ce journal (deux temps)**: la version d'origine de cette entrée annonçait « 6 fichiers de `documentation/explanations/`, 10 de `documentations/tui/` » ; une révision intermédiaire a « corrigé » ce chiffre en **25 porteurs de liens** — c'était **une régression**, produite par `git grep -l` sur la chaîne littérale, qui compte aussi les fichiers où elle n'apparaît qu'en prose. Nouvelle mesure, exécutée sur l'état commité : l'estimation d'origine était **juste** (16 fichiers, 120 liens). Deux acquis malgré tout : le « 5 liens dans `.GCC/main.md` » de la version d'origine était faux (`main.md` porte **0** lien, seulement des mentions en prose et le motif cité entre accents graves dans la présente phrase), et les fichiers `codex.ts`, `test_codex_connection.ts`, `distribution_hive_mind.md`, `bot.e2e.test.ts`, `bashTool.test.ts` imputés à tort à la population 1 relèvent bien de la population 2.
- **Impact**: documentation de référence inutilisable hors de ce poste ; les liens ne cassent rien à l'exécution, mais `AUTH_FILE_PATH` de `codex.ts` dépend d'un chemin de compte personnel pour résoudre un jeton. Le périmètre de réécriture est donc étroit : 16 fichiers de documentation, pas les journaux.
- **Fix requis**: réécrire les 120 liens des 16 fichiers en chemins relatifs au dépôt (les 45 cibles résolubles passent directement), arbitrer le sort des 20 orphelines (renvoi vers le dépôt TUI ou suppression du lien), et remplacer `AUTH_FILE_PATH` de `codex.ts` par une variable d'environnement ou une résolution depuis `os.homedir()`. Les 9 fichiers à mention en prose ne se réécrivent pas : ce sont des records historiques qui nomment un chemin sans le citer en lien. Non commencé : hors du périmètre demandé cette session. **Méthode reproductible**: compter les *liens* avec le préfixe complet `file:` + `//` + chemin absolu du dépôt fantôme, jamais `git grep -l` sur le seul nom du dépôt (il agrège les mentions en prose) ; tester ensuite chaque cible distincte avec `os.path.exists`. Contrôles : 135 mentions littérales, 120 liens, 16 fichiers lien-porteurs, 65 cibles distinctes.

- [2026-08-07] **Bug Anthropic adapter: `role: 'tool'` envoyé tel quel à l'API**
- **Fichiers concernés**: `src/providers/adapters/anthropic.ts` (L175-180), `src/providers/families/protocols/AnthropicCompatibleProtocol.ts`
- **Symptôme**: Les erreurs d'outils (`role: 'tool'`) sont envoyées dans un format incompatible avec l'API Anthropic. Le LLM les interprète comme des messages utilisateur.
- **Fix requis**: Converter `role: 'tool'` → `user` message avec `tool_result` content block (`tool_use_id`, `content`). Vérifier aussi les providers compatibles Anthropic.
- **Tâche Google Tasks**: créée (id: `dk1CY1V2eWF3S184RktJRg`)

- [2026-08-08] **Déconnexion Architecturale Critique : Smart Layer (Layer 0/Layer 1) non instanciée en production**
- **Fichiers concernés**: `src/providers/index.ts` (`providerRouter`), `src/core/ServiceContainer.ts`, `src/providers/layer1/SmartLayer.ts`, `src/providers/layer0/ExecutionLayer.ts`
- **Symptôme**: Le vrai moteur de production appelé par `ServiceContainer` et plus de 20 services (`Planner`, `SubAgentEngine`, `ActionEvaluator`, `memory.ts`, `dreamService`, etc.) reste l'ancien `providerRouter` dans `src/providers/index.ts`. `SmartLayer` / `layer0` / `layer1` n'existent qu'isolés dans leur propre dossier et leurs tests — ils ne sont jamais instanciés par l'application.
- **Cause Racine**: Absence de vérification par graphe de dépendances (`graphify`) ou par sous-agent critique lors des sessions précédentes.
- **Action**: L'utilisateur rédige un plan d'intégration complet. Aucune modif directe avant validation de ce plan.

- [2026-08-07] **Inventaire du système de gestion des erreurs**

- **Architecture**: 3 couches — `toolErrors.ts` (classes), `toolExecution.ts` (pipeline Zod), `core/index.ts` (3 niveaux de catch)
- **Classification**: 5 types (TIMEOUT, NETWORK, AUTH, NOT_FOUND, RATE_LIMIT) + graceful degradation
- **Flow**: `Plugin → _executeTool (classify) → _safeExecuteTool (catch global) → _executeToolWithValidation (format role:tool) → Provider adapter`
- **Validation Zod**: `ValidationRetryError` formatée en XML `<tool_use_error>` pour retry loop
- **Constat**: Système fonctionnel mais le format wire (`role: 'tool'`) est incompatible Anthropic. Même bug que ci-dessus.

## 🌿 Active Branches / Plans

- ~~`plan_tui_extraction`~~ : [COMPLÉTÉ] Extraction et Découplage de la TUI dans un Dépôt Autonome (plan de travail conservé hors dépôt, dans le répertoire de session de l'agent qui l'a produit)
- `plan_unified_context_regulation` : [Audit & Spécification d'Architecture Unified Context Regulation](branches/plan_unified_context_regulation.md)
- `plan_startup_options_menu` : [Menu d'Options UX Interactif CLI au Démarrage](branches/plan_startup_options_menu.md)
- `plan_provider_protocol_families` : [Restructuration du Routeur de Providers par Familles (Protocol & Headers)](branches/plan_provider_protocol_families.md)
- `plan_generation_capabilities` : [Système de Paramètres de Génération Unifiés (GenerationParams)](branches/plan_generation_capabilities.md)
- `plan_smart_provider_two_layer_architecture` : [Architecture Smart Provider à 2 Couches (Layer 1 Intelligente x Layer 0 Fil) & Conversion GenerationParams](branches/plan_smart_provider_two_layer_architecture.md)
- ~~`plan_architectural_review_providers`~~ : [COMPLÉTÉ] Epic MessageConverter Unifié + ConfigValidator (F1, F7, F8, F9)

## 📈 Current Status

- ✅ Done: **Extraction & Découplage TUI Standalone** (dépôt sibling `HIVE-MIND-TUI`) validé 100% (M1-M4 : transport Core isolé, 17 dépendances purgées, 372 fichiers TUI migrés avec docs/plans, 11/11 tests E2E cross-process passés).
- ✅ Done: Menu d'options CLI au démarrage, fix WhatsApp (5 fixes validés runtime), retrait QR auto, architecture Smart Provider 2 couches, GenerationParams.
- ✅ Done: **Réécriture system prompt hive-mind** via flotte 11 agents. Guide_final (1339 lignes) + prompt nouveau (301 lignes) + breakdown (587 lignes) + analyse codebase (759 lignes). Non-committé.
- ✅ Done: **Fix perte de session WhatsApp après arrêt/restart** : 5 causes racines identifiées, 5 fixes appliqués, 4 reviewers adversariaux, audit invariants 5/5 PASS. `tsc` 0, `eslint` 0/0.
- ✅ Done: **8 fixes chirurgicaux providers** : F2 (Cohere tool_call_id), F3 (codestral-latest), F4 (collision ModelRegistry), F5 (guard gemini-native), F6 (randomUUID), F10a/b (fallbacks morts), F11 (JSDoc). Validé tsc/eslint.
- ✅ Done: **Epic MessageConverter unifié** (F1, F7, F8, F9) : convertisseur unifié + ConfigValidator + 4 adapters refactorisés. Audit adversarial 6/6 défauts corrigés. Validé tsc/eslint.
- ✅ Done: **6 Cascades de défaillance long-running corrigées + commit** (`0420867`) : C1 (wake storm), C2 (boucle 440), C3 (amnésie TTL), C4 (zombies), C5 (callbacks dupliqués), C6 (429 cascade + CB bloqué). Critique adversarial APPROVED. 8/8 couches pre-commit passées. Sécurité: node-gtts→google-tts-api, safeFs migrations, npm audit 0 high/critical.
- ✅ Done: **Baseline de gouvernance (6 piliers) déployée et committée** (`0ead9b5` nettoyage + `.gitignore` + husky ; `84e61d2` 62 fichiers : AGENTS.md, ARCHITECTURE.md, `.GCC/`, `.gouvernance/`, `.githooks/`, `.github/`). Second commit posé sous `--no-verify` sur autorisation explicite du mainteneur, depuis remboursé par le correctif ci-dessous.
- ✅ Done: **Quality Gate assainie et durcie** (`cc4fa2a`) : scans littéraux limités au code, scan PEM absolu ajouté, `gitleaks` installé (8.30.1, checksum vérifiée) et rendu bloquant au `pre-commit` (index) comme au `pre-push` (historique) via `_common/detect-secrets.sh` et `.gitleaks.toml`. Commit passé par la gate, sans contournement.
- ✅ Done: **Licence Apache-2.0 et attribution `leandre755`** appliquées dans `LICENSE`, `package.json`, son miroir `license` dans `package-lock.json` et `README.md` (`74aefe7`, via le canal documenté `ALLOW_CONFIG_EDIT=1`, gate exécutée) ; **titulaire du copyright** ajouté à l'annexe `LICENSE` l. 190 (`4955cac`) ; **`AGENTS.md` §6 aligné sur les hooks réels**, en anglais (`8564779`) ; **badge et prérequis Node du README** portés à 22 (`3ea25ce`).
- ⏳ Pending: Dette des chemins absolus machine dans `documentation/` et `documentations/` (voir §Known Bugs) ; arbitrage mainteneur sur `node-version: '20'` de `.github/workflows/release.yml` face à `engines.node >= 22` (§Known Bugs) ; suppression ou câblage des 2 scripts morts de `.githooks/_common/` ; plan d'intégration Smart Layer (Layer 0/Layer 1) non démarré ; Tests unitaires messageConverter/configValidator, tests d'intégration et E2E Jest, test runtime WhatsApp, déploiement Railway.


## 👉 Next Session Direction
La gate n'est plus un blocage : elle est repassée en mode normal et durcie par gitleaks. Reprendre la dette ouverte en priorité — (1) réécrire en chemins relatifs les 120 liens `file:///…/HIVE-MIND-RAILWAY/…` portés par 16 fichiers de documentation : les 45 cibles résolubles telles quelles passent directement, les 20 orphelines (19 `src/tui/**` + `PROJECT.md`) sont à faire arbitrer par le mainteneur ; (2) sortir le chemin de compte personnel codé en dur dans `src/providers/adapters/codex.ts` ; (3) trancher les deux scripts morts de `.githooks/_common/`. Le plan d'intégration Smart Layer reste en attente de rédaction par le mainteneur. Détail de la transition dans `.GCC/resume.md`.
