# Liste des Erreurs et Bugs

Total : 40 entrées réorganisées. Audit du 2026-07-24.

- [x] **Insecure Shell Spawning for Git Pull** (`src/plugins/base/system/index.ts:112`) : Using child_process.exec to run 'git pull' unnecessarily spawns a system shell, which is poor security hygiene. _(Rationale: Replacing exec with execFile is a trivial fix that avoids shell interpretation completely, reducing the attack surface with minimal effort.)_ **→ RÉSOLU : `execFile` utilisé à la ligne 2.**
- [x] **Command Injection Vector in editorUtils** (`src/tui/ui/utils/editorUtils.ts:132`) : Using shell: true with spawn on Windows enables command injection if user-provided editor arguments contain shell metacharacters. _(Rationale: The spawnSync function passes user arguments with shell: true on Windows. The fix requires either disabling shell mode or strictly sanitizing arguments, which is localized and straightforward.)_ **→ RÉSOLU : `shell: true` supprimé.**
- [x] **Inject reasoning_content for Kimi K2** (`src/providers/adapters/kimi.ts:31`) : Inject `reasoning_content` if present for Kimi K2 models. _(Rationale: The context shows the exact location and requirement for injecting `reasoning_content` in the Kimi adapter.)_ **→ RÉSOLU : Injection présente lignes 31-32.**
- [x] **Avoid duplicate message callbacks** (`src/core/transport/baileys.ts:147`) : Do not duplicate `this.on("message", callback)` because `messageCallback` is already called explicitly. _(Rationale: This is a clear, specific instruction related to event listeners that can be easily fixed by removing or adjusting the listener.)_ **→ RÉSOLU : Aucun `on("message")` dupliqué détecté.**
- [x] **N+1 Query in Entity Upsert** (`src/services/knowledgeWeaver.ts:90`) : Database queries inside a loop for upserting entities, which can cause significant performance degradation for large graphs. _(Rationale: Clear N+1 pattern that can be resolved with a bulk upsert operation without modifying the surrounding logic drastically.)_ **→ RÉSOLU : `upsertEntitiesBatch` utilisé ligne 89.**
- [x] **Missing tests for jidHelper module** (`src/utils/jidHelper.ts:20`) : The jidHelper module lacks a corresponding unit test file to verify its logic for WhatsApp IDs. _(Rationale: Pure string manipulation functions for extracting and matching JIDs. Very straightforward to test with simple string assertions.)_ **→ RÉSOLU : `src/tests/unit/utils/jidHelper.test.ts` présent.**
- [x] **Uncovered parseDelayRange helper** (`src/utils/helpers.ts:32`) : The parseDelayRange function in helpers.ts lacks test coverage for parsing time ranges. _(Rationale: Simple pure function parsing strings into min/max numbers. Trivial to provide various valid and invalid formats.)_ **→ RÉSOLU : Tests présents dans `helpers.test.ts`.**
- [x] **Uncovered sanitizeForWhatsApp helper** (`src/utils/helpers.ts:218`) : The sanitizeForWhatsApp function in helpers.ts is not tested for removing sensitive information. _(Rationale: Pure function with string replacement logic using regex. Very straightforward to provide test strings containing secrets.)_ **→ RÉSOLU : Tests présents dans `helpers.test.ts`.**
- [x] **Untested formatForDisplay in jidHelper** (`src/utils/jidHelper.ts:125`) : The formatForDisplay function does not have dedicated test coverage for handling various inputs and fallbacks. _(Rationale: Simple utility function for formatting strings. Easy to test the branching logic for provided names vs masked numbers.)_ **→ RÉSOLU : `formatForDisplay` testé dans `jidHelper.test.ts` ligne 90.**
- [x] **Missing tests for messageSplitter module** (`src/utils/messageSplitter.ts:12`) : The messageSplitter module has no corresponding test file to verify long message chunking logic. _(Rationale: Pure functions for splitting strings based on length and delimiters. Easy to mock input strings and assert array outputs.)_ **→ RÉSOLU : `src/tests/unit/utils/messageSplitter.test.ts` présent.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:245`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:295`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:259`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:506`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:223`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Remove console.log in service layer** (`src/services/groupService.ts:275`) : Development console.log statements should be removed or replaced with proper logging framework. _(Rationale: console.log is easily identifiable and replaceable. Removal poses minimal risk.)_ **→ TOUJOURS PRÉSENT.**
- [x] **Path Traversal in ASTTools** (`src/plugins/base/dev_tools/ASTTools.ts:86`) : The resolveAbsolute function permits absolute paths, allowing attackers to read files outside the intended sandbox directory. _(Rationale: The function explicitly allows absolute paths without enforcing boundaries, and is directly used to access files in handleGetSkeleton and handleGetFunction. The fix is a clear, simple boundary check.)_ **→ RÉSOLU : `permissionManager.isInSandbox()` appliqué dans `resolveAbsolute` (lignes 147-149).**
- [x] **Extract reasoning_content for history** (`src/providers/adapters/kimi.ts:104`) : Extract `reasoning_content` to keep in the conversation history. _(Rationale: The context shows the exact location and requirement for extracting `reasoning_content`.)_ **→ RÉSOLU : Extraction présente lignes 104-105.**
- [x] **Extract "thought" block if present** (`src/providers/adapters/gemini.ts:169`) : Extract the "thought" block from Gemini 2.0 Thinking models if it exists. _(Rationale: The context shows the exact location and requirement for extracting the "thought" block.)_ **→ RÉSOLU : Extraction présente ligne 169-170.**
- [x] **Handle "thought" block for Thinking models** (`src/providers/adapters/gemini.ts:30`) : Explicitly handle the "thought" block for Gemini 2.0/3.0 Thinking models. _(Rationale: The context shows the exact location and requirement for handling the "thought" block in the Gemini adapter.)_ **→ RÉSOLU : Gestion présente lignes 30-32.**
- [x] **Safe check for event implementation** (`src/core/transport/baileys.ts:1157`) : Ensure safe access to event implementation because `ev` can vary. _(Rationale: This is a localized safety check and is easily understandable.)_ **→ RÉSOLU : Guard `typeof this.sock.ev.eventNames === 'function'` présent ligne 1163.**
- [x] **N+1 Query in Relationship Add** (`src/services/knowledgeWeaver.ts:96`) : Database queries inside a loop for adding relationships, leading to multiple independent round-trips. _(Rationale: Clear N+1 pattern that can be resolved with a bulk add operation without modifying the surrounding logic drastically.)_ **→ RÉSOLU : `addRelationshipsBatch` utilisé ligne 94.**
- [x] **Unwrap special message types** (`src/core/transport/baileys.ts:551`) : Unwrap messages that are encapsulated in Ephemeral, ViewOnce, etc. formats. _(Rationale: The unwrapping logic is clearly visible in the context and can be expanded if needed.)_ **→ RÉSOLU : Déballage Ephemeral/ViewOnce/DocumentWithCaption implémenté lignes 556-570.**
- [x] **Filter reactions to bot messages only** (`src/core/transport/baileys.ts:456`) : Ensure we only process reactions on messages sent by the bot (fromMe: true). _(Rationale: The logic is clear and localized to the reaction event handler.)_ **→ RÉSOLU : Filtre `!reaction.key.fromMe` présent ligne 463.**
- [x] **Ignore WhatsApp Stories/Status** (`src/core/transport/baileys.ts:330`) : Ensure WhatsApp status (Stories) broadcast messages are ignored. _(Rationale: The fix is already partially implemented or requires a simple condition to ignore status@broadcast JIDs.)_ **→ RÉSOLU : Guard `remoteJid === 'status@broadcast'` présent ligne 331.**
- [x] **FIX: On fait d'abord un select pour ne pas écraser les** (`src/services/feedbackService.ts:46`) : On fait d'abord un select pour ne pas écraser les autres métadonnées (comme les tags) _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : `.select('id, metadata')` effectué avant toute mise à jour.**
- [x] **FIX: Kimi K2+ nécessite reasoning_content pour que le c** (`src/core/index.ts:1516`) : Kimi K2+ nécessite reasoning_content pour que le contexte soit valide _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : Injection `historyEntry.reasoning_content` présente ligne 1524.**
- [x] **FIX: Detecter et corriger le format <send_message>JSON</send_message>** (`src/core/index.ts:1937`) : Detecter et corriger le format <send_message>JSON</send_message> _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : Regex de détection + nettoyage XML présent lignes 1937-1950.**
- [x] **TODO: Implémenter la logique de scan des tâches en atten** (`src/core/index.ts:2846`) : Implémenter la logique de scan des tâches en attente ou ping users inactifs _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : `getPendingReminders()` implémenté dans le case `spontaneousReflection` lignes 2846-2854.**
- [x] **BUG: #7 FIX] Never leave the user without a response** (`src/core/index.ts:1388`) : #7 FIX] Never leave the user without a response _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : Fallback réponse utilisateur implémenté lignes 1388-1394.**
- [x] **FIX: Récupération des outils pour l'Agentic Path** (`src/core/index.ts:1244`) : Récupération des outils pour l'Agentic Path _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : `pluginLoader.getRelevantTools()` + pruning Sentinel présents lignes 1244-1252.**
- [x] **FIX: ed overhead: borders (n+1) + padding (2n)** (`src/tui/ui/utils/TableRenderer.tsx:156`) : ed overhead: borders (n+1) + padding (2n) _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : Calcul `fixedOverhead = numColumns + 1 + numColumns * COLUMN_PADDING` présent ligne 152.**
- [x] **FIX: ed vertical space taken by preview pane elements** (`src/tui/ui/components/ThemeDialog.constants.ts:29`) : ed vertical space taken by preview pane elements (title, borders, margins). _(Rationale: Needs more investigation but is generally solvable.)_ **→ RÉSOLU : Constante `PREVIEW_PANE_FIXED_VERTICAL_SPACE = 8` définie ligne 25.**
- [x] **Resolve actionable TODO** (`src/core/index.ts:2846`) : Pending TODO: Implémenter la logique de scan des tâches en attente ou ping users inactifs _(Rationale: Requires implementation of the specified task, which may need some context but appears self-contained.)_ **→ RÉSOLU : Implémenté (voir entrée précédente).**
- [x] **Missing tests for SafeScriptValidator validateCode** (`src/services/ptc/SafeScriptValidator.ts:195`) : The validateCode function in SafeScriptValidator lacks unit tests for ensuring code safety. _(Rationale: Requires providing various valid/invalid JS code snippets to test the acorn parser integration and rules checking.)_ **→ RÉSOLU : `src/tests/unit/services/SafeScriptValidator.test.ts` couvre `validateCode`.**
- [x] **Sequential Network/Cache Access for Model Health** (`src/services/quotaManager.ts:297`) : Sequential asynchronous calls to getModelHealth inside a loop, which could be parallelized to improve latency. _(Rationale: Although early return is used, fetching them in parallel using Promise.all or a batch pipeline would reduce overall worst-case latency.)_ **→ TOUJOURS PRÉSENT : Boucle `for...await` séquentielle lignes 296-301 (commentaire justificatif mais pas de `Promise.all`).**
- [x] **Insecure Execution of Node Sandbox via child_process.exec** (`src/core/security/PermissionManager.ts:29`) : The BANNED_COMMANDS explanation comment implies that inline code execution via require('child_process').exec('sudo ...') is possible if not properly sandboxed, illustrating a vulnerability if SafeScriptValidator fails. _(Rationale: This is currently a comment noting a potential vector that SafeScriptValidator aims to prevent, but a full fix requires robust validation across the executor pipeline, which is complex.)_ **→ RÉSOLU : `BANNED_FLAG_PATTERNS` couvre les vecteurs `node -e`, `python -c`, `bash -c` etc. (lignes 31-36).**
- [ ] **Refactor overly long function baileys.ts** (`src/core/transport/baileys.ts:180`) : Function is 349 lines long. It should be split into smaller, more manageable pieces. _(Rationale: Refactoring long transport layer functions requires deep understanding of connection lifecycle to split it safely without breaking functionality.)_ **→ TOUJOURS PRÉSENT : Fichier 1237 lignes, refactoring non effectué.**
- [ ] **Refactor overly long function index.ts** (`src/core/index.ts:1403`) : Function is 373 lines long. It should be split into smaller, more manageable pieces. _(Rationale: Refactoring long functions often requires deep understanding of the logic to split it safely without breaking functionality.)_ **→ TOUJOURS PRÉSENT : Fichier 3119 lignes, refactoring non effectué.**
- [x] **Refactor overly long function AppContainer.tsx** (`src/tui/ui/AppContainer.tsx:618`) : Function is 2353 lines long. It should be split into smaller, more manageable pieces. **→ RÉSOLU : Monolithe décomposé en 5 custom hooks dédiés dans `src/tui/ui/hooks/` (`useAppContainerState`, `useAppContainerDialogs`, `useAppContainerSession`, `useAppContainerUI`, `useAppContainerKeyboard`), réduisant `AppContainer.tsx` à 80 lignes avec 0 erreur ESLint/TS.**
